_BASE_: "../base/faster_rcnn_R_50_FPN.yaml"
AL:
  DATASET:
    IMAGE_BUDGET: 160
  OBJECT_FUSION:
    BUDGET_ETA: 0.4
    INITIAL_RATIO: 0.80
    LAST_RATIO: 0.30
    REMOVE_DUPLICATES_TH: 0.25
  TRAINING:
    EPOCHS_PER_ROUND_INITIAL: 320
    EPOCHS_PER_ROUND_LAST: 150
    ROUNDS: 8
MODEL:
  ROI_HEADS:
    NUM_CLASSES: 5
    SCORE_THRESH_TEST: 0.5
SOLVER:
  IMS_PER_BATCH: 8
TEST: 
  DETECTIONS_PER_IMAGE: 150